# Tracking
Build reusable MediaPipe hand and pose tracking here. City games should consume simple values such as hand coordinates, `isJumping`, and pedal progress rather than access MediaPipe directly.
